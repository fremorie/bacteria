import styled from "styled-components";

const tubeHeight = "300px";
const tubeWidth = "500px";

export const Canvas = styled.canvas`
  height: ${tubeHeight};
  width: ${tubeWidth};
  border: 2px solid black;
  border-radius: 25px;
`;
